export default function AboutPage() {
  return (
    <section className="grid gap-3">
      <h1 className="text-2xl font-semibold">About</h1>
      <p>This is a minimal Vite + React + TS + Tailwind starter.</p>
    </section>
  );
}
